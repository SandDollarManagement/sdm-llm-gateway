# SDM LLM Gateway — Open Features (backlog)

Features/ideas that exist or are wanted but whose intent is not yet specced.
One entry per idea as `OF-NNN`. Each graduates into a `FEATURE_SPECS.md` `FS-NNN`
once discussed with the operator — never auto-promote, never invent its intent.

---

_No backlog items yet._
